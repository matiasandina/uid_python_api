# Example TTL Session Output

This folder contains a tiny example output set generated by the TTL capture format.

- `ttl_raw.bin`: sample payload bytes in stream order
- `ttl_meta.json`: metadata schema example

Use this as a format reference only (not real experimental data).
